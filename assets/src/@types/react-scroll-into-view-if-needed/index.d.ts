declare module 'react-scroll-into-view-if-needed' {
  import * as React from "react";

  export default class ScrollIntoViewIfNeeded extends React.Component<Props, {}> {
  }

  export interface Props {
    elementType?: string;
    active?: boolean;
  }
}
